import React from 'react';

const SettingsFormTitle = ({ title }) => {
    return (
        <div className="col-12">
            <h4 className="sign__title">{title}</h4>
        </div>
    );
};

export default SettingsFormTitle;